#include <iostream>
#include <string>
#include <vector>

void print(std::string str, std::vector<int> v) {
    std::cout << str << std::endl;
    for (auto &i : v) {
        std::cout << i << " ";
    }
}

int main(int argc, char **argv) 
{
    print("jiban", {1, 3, 5, 7});
    return 0;
}
